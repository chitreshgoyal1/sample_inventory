<%= form_for(@device_carrier) do |f| %>
  <% if @device_carrier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@device_carrier.errors.count, "error") %> prohibited this device_carrier from being saved:</h2>

      <ul>
      <% @device_carrier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :device_carrier %><br />
    <%= f.text_field :device_carrier %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
