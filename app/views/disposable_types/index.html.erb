<h1>Listing disposable_types</h1>

<table>
  <tr>
    <th>Disposable type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @disposable_types.each do |disposable_type| %>
  <tr>
    <td><%= disposable_type.disposable_type %></td>
    <td><%= link_to 'Show', disposable_type %></td>
    <td><%= link_to 'Edit', edit_disposable_type_path(disposable_type) %></td>
    <td><%= link_to 'Destroy', disposable_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Disposable type', new_disposable_type_path %>
