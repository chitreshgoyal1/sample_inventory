<%= form_for(@device_status) do |f| %>
  <% if @device_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@device_status.errors.count, "error") %> prohibited this device_status from being saved:</h2>

      <ul>
      <% @device_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :device_status %><br />
    <%= f.text_field :device_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
